{{ if (and (eq .chezmoi.os "linux") (not .ephemeral) (not .headless)) -}}
#!/bin/bash

set -eufo pipefail

fc-cache

{{ if .i3wm -}}
cat ~/.config/dconf/user.d/* | dconf load /
{{ end -}}

{{ end -}}
